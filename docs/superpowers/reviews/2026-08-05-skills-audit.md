# Skills Audit — 2026-08-05

Method: six parallel read-only subagent audits against the writing-skills
checklist (see docs/superpowers/plans/2026-08-05-skills-hardening.md, Task 1).
Raw subagent reports: session scratchpad `audit-<skill>.md`. Categories were
re-checked by the orchestrator: any body-text change to an injected skill
(fmea, gherkin-sv, requirements-quality) is **behavior**, as is any
description change that adds trigger phrasing (it can alter self-invocation).
Recategorized on that rule: GS-6, GS-7, GS-8 (form → behavior).

Severity-weighted totals per skill (high/medium/low): execute-flow 2/6/4,
requirements-quality 2/3/4, gherkin-sv 2/4/2, fmea 1/3/6, trace-check 2/5/3,
install-toolkit 2/3/4. 58 findings; 34 behavior, 24 form.

## Impact order for the fix phase

1. **execute-flow** — orchestrates every run; two high behavior defects
   (contradictory Traces instruction, workflow-summarizing description).
2. **requirements-quality** — injected into ~15 authoring steps + all QA
   sub-audits; UR-format contradiction generates false findings each run.
3. **gherkin-sv** — two high defects make the only example unreachable and
   mis-tagged, producing workbooks that deterministically fail trace-check.
4. **fmea** — Prevention/Mitigation column mismatch conflicts with the
   workbook template in every FMEA step.
5. **trace-check** — description invites acting without the body; missing
   partial-run guidance causes mid-flow false-error churn.
6. **install-toolkit** — setup-only; PATH-refresh gap can fail installs.

## Cross-skill coordination

- **EF-5 ↔ TC-2/TC-5**: execute-flow sheds its duplicated trace-check
  invocation/ignore-list; trace-check gains the partial-run section and drops
  its duplicated audit-loop orchestration. Apply as one coordinated pair
  (execute-flow task and trace-check task each own their side).
- **GS-2 + GS-6**: one combined fix — inline the correct `@ID:RQ_FN_01`
  example into the skill and demote `Example.feature` to layout-only
  reference (or drop it).
- **GS-4**: the preferred variant edits the workbook template
  (`flows/expectationeering-flow/expectationeering-workbook.md`), which is
  outside this sub-project's scope (six skill files only). In-scope variant:
  cut the duplicated lines from the skill. Decision at the approval gate.
- **RQ-7**: touches `.claude/agents/quality-assurance.md` (deletes a sentence
  duplicated from the injected body) — one line outside the skill files,
  flagged here explicitly.
- **IT-8**: documents that the PATH-refresh snippet duplication with
  execute-flow is intentional; no execute-flow change.

## Approval decisions (Eric, 2026-08-05)

All recommended behavior findings approved per skill; three deferred by
decision: **RQ-3** and **GS-8** (trigger-phrased descriptions on injected
bodies risk spurious self-invocation — house style kept) and **GS-4** (the
correct fix edits the workbook template, which is outside this sub-project's
scope). Impact order confirmed as listed above.

## Findings

Status values: `open` → `approved`/`deferred (…)` (behavior, Task 3) →
`fixed` / `deferred (not reproduced)` / `not resolved` / `n/a` (Tasks 4–9).

### execute-flow

| ID | Category | Severity | Location | Finding | Proposal | Status |
|----|----------|----------|----------|---------|----------|--------|
| EF-1 | behavior | high | frontmatter description | Description is a first-person process summary ("Orchestrate… ingest… drive… assemble…"); trigger clause buried after 30 words. Agents may act on the summary without reading the body. | Replace with trigger-only, third person: `Use when the user asks to execute, run, or start an EE-Toolkit flow under flows/ — e.g. "Execute flows/expectationeering-flow", "start expectationeering", "run expectationeering", or just a bare flow name matching a folder in flows/. Also use when the user asks to (re)produce a flow's workbook, .docx output, or run statistics.` | fixed |
| EF-2 | behavior | high | line 46 (conventions preamble) | Preamble injected into every agent prompt says to fill `Traces`, contradicting flow.md:79 (steps 1a–1d leave `Traces` blank until 1e) and the skill's own step-4 ignore-list. Authors will invent DC references. | Amend preamble: "…record the upstream ID(s) … in its `Traces` column (leave `Traces` blank if this step's artifact description says the traces are completed by a later consolidation step)…" and add to step 3: `For steps 1a–1d, state explicitly in the prompt: leave the Traces column empty; step 1e fills it.` | fixed |
| EF-3 | behavior | medium | line 86 (step 7 run-stats) | Instructs writing "agents ran sequentially, so summed duration approximates wall-clock" — false under the skill's own parallel groups and contradicted by the cited reference run-stats. | Replace with: "A one-line note that the figures are subagent totals (excluding orchestrator usage and wait-for-permission time), and that because parallel groups ran concurrently the summed duration exceeds wall-clock — report both." Add `wall-clock duration` as a Totals row. | fixed |
| EF-4 | form | medium | lines 16–21, 25 | "Accepted start commands" restates the description and CLAUDE.md:22; todo-list paragraph restates CLAUDE.md:11 near-verbatim. | Delete lines 16–21. Compress line 25 to: `Per CLAUDE.md, show progress as a live todo list. Items: one per flow phase, plus Ingest input documents, Quality Assurance audit, Convert to .docx, Write run statistics. Exactly one in_progress; mark completed when that step's output is written to the workbook.` (~110 words saved) | fixed |
| EF-5 | form | medium | lines 69, 97 | trace-check invocation, `--json` flag, ignore-list and routing spelled out twice here and a third time in the trace-check skill. | Step 4 keeps only: "After each numbered phase, run the trace-check skill against the workbook and fix any error that concerns already-authored artefacts by re-spawning the owning agent. Ignore findings caused by steps that have not run yet (blank expectation Traces before 1e, missing SV coverage before 9f–9h, placeholder warnings)." Audit bullet keeps only: "Run the trace-check skill first and iterate to zero errors before spawning any audit agent; route findings per that skill's routing table." Drop both inline command strings. | fixed |
| EF-6 | form | medium | lines 84–90 (step 7) | Run-stats required elements are six prose bullets — output-shape guidance that should be a structural template. | Replace bullets with a fenced skeleton: `# Run Statistics — <flow name>`, `**Flow**/**Workbook**/**Date**/**AI model**` lines, figures note, `## Totals` fixed metric rows, `## Per-step breakdown` header `\| Step \| Role \| Tokens \| Duration (s) \|`, `## Notes` three fixed bullets; plus `Format reference: outputs/2026-07-04-170434/run-stats.md.` | fixed |
| EF-7 | form | medium | lines 44–67 (step 3) | Which body to inject at which step sits in three prose bullets — the most-consulted content, unscannable. | Add quick-reference table `\| Steps \| Inject skill body \|`: 9f–9h + SV sub-audit → gherkin-sv; 6d, 6e, 6h, 6i + usability-chain sub-audit → fmea; every BR_*/UR_*/USR_*/RQ_* authoring/co-author step + all four QA sub-audits → requirements-quality. Delete the three prose bullets. | fixed |
| EF-8 | form | medium | lines 53–61 | The two most skip-prone rules (never pass whole workbook; agents never edit the workbook) are bare prohibitions without counters. | Add red-flags table `\| If you catch yourself thinking \| Reality \|`: "This step is complex, safer to pass the whole workbook" → read flow.md's Traces from X → Y lines, then scope; "Just this once, let the agent edit the file" → agents have tools: Read only — an edit request fails and burns the invocation; "The agent asked for a section I did not plan for" → pass that one section on demand; that is the designed path. | fixed |
| EF-9 | form | low | line 104 | "Cap the loop at a reasonable number of cycles" is unenforceable. | Replace with: `Cap at 3 remediation cycles. If findings persist after the third, stop and surface the outstanding findings with their Owners in your final message.` | fixed |
| EF-10 | form | low | lines 99–102 | Asymmetric injection reminders in the sub-audit list (only item 4 names its skill body). | With the EF-7 table in place, delete "with the `gherkin-sv` skill body" from item 4; add: `Inject skill bodies into each sub-audit per the injection table above.` | fixed |
| EF-11 | behavior | low | line 44 | `subagent_type` "matching the role" leaves the Role→agent-name mapping implicit. | Make explicit: "subagent_type is the flow.md Role lowercased with spaces replaced by hyphens (User Stakeholder → user-stakeholder), matching the name in .claude/agents/." | deferred (not reproduced: 3/3 baseline reps inferred user-stakeholder correctly without the rule) |
| EF-12 | form | low | lines 31, 33 | No rule for zero/multiple flow-name matches; "primary input document" undefined with multiple inputs. | Step 1 add: "If the requested name matches no folder under flows/, list the available flows and stop. If it matches several, ask which one." Step 2 add: "Primary input = the single .docx in inputs/; if there are several, the one the user named, else the largest — state your choice before creating the output folder." | fixed |

### requirements-quality

| ID | Category | Severity | Location | Finding | Proposal | Status |
|----|----------|----------|----------|---------|----------|--------|
| RQ-1 | behavior | high | lines 21, 25 vs flow.md:134 | Criterion 10 + quick-check mandate "shall" + system subject, contradicting the mandated UR_* user-story form; QA can flag conforming URs. | Row 10 Definition adds: `**UR_* exception:** user requirements keep the flow's mandated user-story form (As a <user group> I want <feature> so that <benefit>) — Conformant there means the named user group is explicit and the form is used consistently, not that "shall" appears.` Line 25: replace `"shall" + the system as subject (10)` with `"shall" + the system as the explicit subject — for UR_* instead the mandated user-story form with a named user group (10)`. | fixed |
| RQ-2 | behavior | high | after line 21 | Pure defect/prohibition list; no positive contract or worked example of a compliant requirement. | Insert `## Compliant shape` with the statement pattern `<When/While <trigger>,> the <subject> shall <one observable action> <quantified bound + unit> <under <measurement condition>>.`, one defective example ("detect occlusion quickly…" failing 1/2/4/8) and its compliant two-row split ("When line pressure exceeds 2.0 bar, the system shall raise an audible occlusion alarm within 5 s." / "The occlusion alarm shall be audible at ≥ 65 dB(A) at 1 m in a 45 dB(A) ambient."). | fixed |
| RQ-3 | behavior | medium | frontmatter | Description is a mechanism summary (injection plumbing), not triggering conditions. | Replace with: `Use when authoring, co-authoring, or auditing a requirement statement in an Expectationeering workbook (BR_*, UR_*, USR_*, RQ_IF_*, RQ_FN_*, RQ_PR_*, RQ_NF_*, RQ_CS_*), or when a review flags vague, untestable, compound, or SMART-failing "shall" statements — the ten INCOSE quality criteria, common defects, and the compliant statement shape.` | deferred (rejected at gate: self-invocation risk, 2026-08-05) |
| RQ-4 | behavior | medium | line 13 (criterion 2) | Blanket "no placeholders" collides with flow-mandated `_To be added_` stubs; sub-audits will raise false Complete findings. | Append to criterion 2 defects cell: "— but the template's flow-mandated placeholders (`_To be added_` diagram stubs, 'Not filled by this flow' notes) are required by the flow and are not Complete defects". | fixed |
| RQ-5 | behavior | medium | line 25 (quick-check) | Pre-return checks packed into one 60-word prose sentence — prose reminder where structural slots are needed. | Replace with per-row checklist: one obligation, no "and"/"or" joining claims (8); number/bound on claim **and** test condition (1, 4); no TBD, no unbounded "upon detecting X" (2); same figures/clock-starts as neighbours (3); cited upstream ID really contains this intent (5); mandated statement form for the prefix — "shall" + system subject, or UR user-story form (10). | fixed |
| RQ-6 | behavior | low | line 29 | First sentence duplicates execute-flow:98–103 and quality-assurance.md:21; dead weight in authoring injections. | Cut first sentence; keep: `## Audit usage` / "Criteria are independent: a requirement that uses 'shall' but is untestable fails Verifiable while passing Conformant. Tag each finding with the violated criterion by name." | fixed |
| RQ-7 | form | low | .claude/agents/quality-assurance.md:44 | Agent file duplicates the criteria-independence sentence verbatim from the injected body (single-source violation). Outside the six skill files — flagged. | Delete quality-assurance.md line 44; sentence stays only in the skill body. | fixed |
| RQ-8 | behavior | low | H1, line 8 | Keyword gaps: no "SMART", "weasel word", "acceptance criteria", "requirements review". | H1 → `# INCOSE Requirements Quality Criteria (SMART requirement checks)`; line 8 append "— these are the SMART/INCOSE checks referenced by the flow's requirement guidance". | fixed |
| RQ-9 | behavior | low | body-wide | No rationalization counters for the most-skipped rules (quantified test condition; splitting compound rows). | Add under quick-check: **Rationalizations that produce findings:** "the threshold is already in the input document" → repeat it in the statement; "splitting this row would duplicate the trace" → duplicate the trace, not the obligation; "the test condition is obvious" → if obvious, cheap to write; unwritten = unverifiable. | fixed |

### gherkin-sv

| ID | Category | Severity | Location | Finding | Proposal | Status |
|----|----------|----------|----------|---------|----------|--------|
| GS-1 | behavior | high | line 8 | Example link `../../../flows/…/Example.feature` is relative to the skill dir; injected into prompts running at repo root it resolves above the repo — the authoritative example is unreachable. | Use plain repo-root paths: "Follow the reference file `flows/expectationeering-flow/Example.feature` …" (also in the line-18 heading). | fixed |
| GS-2 | behavior | high | line 8 vs Example.feature:1 | Skill says follow the reference "including the leading `@ID:` tag", but the file is tagged `@ID:PR_02.1`, not the mandated `@ID:RQ_FN_xx`; check_traces.py:85 then reports missing SV coverage. | Combined with GS-6: inline the correct 16-line example from `expectationeering-workbook.md:313-329` (tagged `@ID:RQ_FN_01`) and demote `Example.feature` to layout/indentation reference only, noting its `@ID:PR_02.1` tag predates the convention. | deferred (not reproduced: 3/3 reps tagged RQ_FN correctly despite the wrong-tagged reference) |
| GS-3 | behavior | medium | line 12 | The `.n` sub-index convention (`@ID:RQ_FN_01.1`) supported by the converter and used by Example.feature is undocumented — agents guess at multi-file coverage. | Append: "If one requirement needs more than one feature file, suffix the tag with a sub-index — `@ID:RQ_FN_01.1`, `@ID:RQ_FN_01.2`; the converter strips the suffix when tracing." | fixed |
| GS-4 | behavior | medium | lines 13–15 vs workbook template:311 | User-story/`Rule:`/coverage sentences near-verbatim duplicate the template's Verification blurb that execute-flow already passes as scoped context — agents receive both copies. Preferred fix edits the template (out of scope); in-scope variant cuts the skill lines. | In-scope variant: cut lines 13–15 from the skill, keeping only skill-unique content (line 12, second half of 14, 16). Alternative (out of scope this sub-project): collapse the template blurb instead. Decision at gate. | deferred (template fix out of scope, 2026-08-05) |
| GS-5 | behavior | medium | lines 15–16 | "every `RQ_*` covered by at least one scenario" has no evidence mechanism: script checks only `RQ_FN_*`, and `# verifies` comments are forbidden while the inline `(RQ_xx)` is optional. | Make evidence mandatory: "Where a scenario also verifies a non-functional, performance, interface, or constraint requirement, name it inline in the step text as `(RQ_PR_01)` — this inline reference, not a `#` comment, is the coverage evidence the QA sub-audit checks." | deferred (not reproduced: 3/3 reps included the inline (RQ_xx) reference) |
| GS-6 | behavior | medium | whole body | No inline example; prose indentation rules are all the agent has when the reference file is unreachable (GS-1) or misleading (GS-2). | Add one fenced `gherkin` block: the `@ID:RQ_FN_01` feature from `expectationeering-workbook.md:313-329` verbatim (user story, one `Rule:`, one `Scenario` with aligned two-row data table). Combined with GS-2. | deferred (not reproduced: coupled to GS-2; layout correct in 3/3 baseline reps) |
| GS-7 | behavior | low | line 27 | Rendering section spends ~60 words on converter internals; only the pipe-alignment clause is actionable, and it restates line 8. | Reduce to: "The converter records each feature file as one `SV_*` row in the Verification table and renders it in fixed-width monospace with syntax colouring, so misaligned data-table pipes are visible in the final .docx — keep them aligned in the markdown." | fixed |
| GS-8 | behavior | low | frontmatter | Description is a content summary + orchestration metadata; sibling-consistent but not trigger-phrased. Cosmetic (injected by path). | Only if aligning all three supporting skills: `Use when authoring, co-authoring, or auditing the SV_* Gherkin BDD feature files in the Expectationeering workbook — the canonical rules for feature structure, @ID tagging, exact indentation, data-table alignment, and converter rendering.` Drop the injection sentence. | deferred (rejected at gate: self-invocation risk, 2026-08-05) |

### fmea

| ID | Category | Severity | Location | Finding | Proposal | Status |
|----|----------|----------|----------|---------|----------|--------|
| FM-1 | behavior | high | lines 22, 38 (rule 3) | `Prevention / Mitigation (existing / new)` bullet assumes both tables split existing/new, but USER_DFMEA has a single `Prevention` column — rule 3 demands a "new" mitigation with no column to hold it. | Split into two bullets: `**Prevention** (USER_DFMEA)` — design measures; additional measures prefixed `NEW:`; `**Mitigation (existing)/(new)** (UFMEA)` — existing = current concept, new = additional demand. Keep the shared "verifiable design property" sentence. Amend rule 3: "…at least one **new** design-level mitigation (`NEW:`-prefixed in `Prevention`, or in `Mitigation (new)`)…". | fixed |
| FM-2 | behavior | medium | lines 15–22 | `Item/Function` (a real USER_DFMEA column) never defined; `Requirement`/`Scenario Title` only glanced at — authors improvise 3 of 11/12 columns. | Add bullet: `**Item/Function** (USER_DFMEA) — the user-facing function or interaction step being analysed (e.g. "sensor placement", "alarm acknowledgement"), not a product component. Requirement (USER_DFMEA) / Scenario Title (UFMEA) restate the traced upstream item in words so the row reads standalone.` | fixed |
| FM-3 | behavior | medium | whole body | No worked example — 627 words of prose rules, zero rows, for a table-authoring skill. | Add `## One worked row` before the rules: one filled USER_DFMEA row + one filled UFMEA row as markdown tables with real template headers, reusing the scattered parenthetical fragments; strip the now-redundant parentheticals. | fixed |
| FM-4 | behavior | medium | lines 32, 36–38 | Severity-downgrade, cause-restates-mode and training-as-mitigation rules are bare prohibitions without rationalization counters. | Add red-flag table: cause reads like the mode → ask *why* it occurred; end-effect stops at the display → follow through to patient consequence; severity lowered because "the clinician would notice" → detection assumption, rate uncontrolled; training/labelling/IFU as mitigation on Critical/High → not a design measure. | deferred (not reproduced: 2/2 reps rejected the downgrade argument under the existing rule) |
| FM-5 | behavior | low | lines 44–46 | Audit-usage section mostly restates "apply the rules above"; only the realisation check is new. | Delete section; append as rule 8: "Every **new** mitigation is realised by a downstream `USR_*`, `RQ_*`, or UI/UX artefact — an unrealised new mitigation is a finding." | fixed |
| FM-6 | behavior | low | lines 10–11 | Table one-liners paraphrase flow.md/template text the agent already receives. | Compress to the unique parts: "User DFMEA (`USER_DFMEA_*`) — operation failures, one per user requirement. `USER_DFMEA → UR`." / "Usability FMEA (`UFMEA_*`) — interaction failures, per use scenario/task. `UFMEA → UT`." Keep the disjointness rule verbatim. | fixed |
| FM-7 | behavior | low | line 39 (rule 4) | First half duplicates flow.md:80's inheritance rule that execute-flow already injects; only the UFMEA→UT→UR hop is new. | Reduce to: "4. UFMEA `Classification` inherits through the UT to the UT's underlying UR — the UT table itself carries no `Classification`." | fixed |
| FM-8 | behavior | low | line 42 (rule 7) | "safety-relevant UR" undefined; scenario-granularity coverage unverifiable against UT-granularity traces. | Rewrite: "7. Coverage: every UR classified Critical/Major appears in at least one USER_DFMEA row; every use scenario has at least one UFMEA row, and each UFMEA `Traces` cites the specific `UT_*` within it." | deferred (not reproduced: 2/2 reps interpreted coverage consistently as Critical+Major) |
| FM-9 | form | low | description | Trailing sentence hardcodes the step list maintained in three files. Keep the non-trigger house style (trigger phrasing risks spurious self-invocation). | Change trailing sentence to: "Injected inline by `execute-flow` into the FMEA authoring/co-author steps and the usability-chain QA sub-audit — single source, no copies in agent files." | fixed |
| FM-10 | form | low | description/body | Thin risk-vocabulary keywords (no "use error", "human factors", "harm"). | Append to description sentence 1: "— use errors, human-factors causes, and harm-based severity." | fixed |

### trace-check

| ID | Category | Severity | Location | Finding | Proposal | Status |
|----|----------|----------|----------|---------|----------|--------|
| TC-1 | behavior | high | frontmatter | Description opens with an imperative summary of what the script does; agents may act on it and skip the body; never names `check_traces.py`. | Replace with: `Use when validating an Expectationeering workbook's traceability and structure — during the Quality Assurance audit (step 10a), after any authoring step or phase that adds rows with a Traces column, or when a workbook may contain dangling or wrong-direction traces, RQ_FN_* with no SV feature coverage, unaddressed DC_* gaps, duplicate or skipped IDs, or leftover fill-in placeholders. Covers the scripts/check_traces.py validator; use it before spending model tokens on a full-workbook QA read.` | deferred (not reproduced: 2/2 baseline reps loaded the skill before acting) |
| TC-2 | behavior | high | missing subsection | Skill invites mid-flow use but never says which findings are *expected* during a partial run (that knowledge lives only in execute-flow:69) — agents will "fix" ~30 bogus errors. | Add `### Expected findings during a partial run`: `RQ_FN_* → no SV feature file` expected until 9f–9h; `UE_/ME_/BE_/RE_ → no upstream trace` expected until 1e; `DC_* gap not addressed` expected until 1e; `unfilled placeholder` expected for unrun sections. Close: "Fix only findings that concern already-authored sections; the rest resolve themselves." Coordinate with EF-5. | fixed |
| TC-3 | behavior | medium | lines 53–56 (routing table) | Routing table incomplete/ambiguous vs script output: missing `UE_/ME_/BE_/RE_` and `template`; `KA_*` in two rows; `SV_*` id never emitted. | Rewrite: UE_*, ME_*, BE_*, RE_*, DC_*, KA_*, BR_*, RQ_FN_* → Product Owner; UC_*, DD_*, IF_*, RQ_IF_*, RQ_PR_* → System Architect; IU_*, MD_*, UR_*, USR_*, UT_*, UFMEA_*, USER_DFMEA_* → Usability Validation; RQ_NF_*, RQ_CS_* → Regulatory Stakeholder; `template` → orchestrator. Drop `SV_*` and the duplicate `KA_*` row; note missing-SV findings arrive under `RQ_FN_*`, fix = new feature file (Product Owner). | fixed |
| TC-4 | form | medium | lines 14–18 | Overview restates the full finding list the table already owns (three copies in 423 words). | Replace with: "Run the mechanical traceability and structure sweep as a script — it costs **zero model tokens** and finds the whole class of defects a full-workbook QA read otherwise burns six figures of tokens on. The LLM audit is for meaning; this script is for structure." | fixed |
| TC-5 | form | medium | lines 58–61 (step 3) | Step 3 duplicates execute-flow's audit-loop orchestration (sub-audit topology is not this skill's contract). | Cut to: "3. Only once the script returns no errors, start the semantic QA audit — this script proves nothing about clarity, atomicity, or verifiability." Coordinate with EF-5. | fixed |
| TC-6 | form | medium | lines 22–23 | Points at the user-scoped `venv-python-for-docx` memory, which covers a different script and dangles on other machines. | Replace with: "On Windows use the project venv interpreter directly; `uv run python` or plain `python` works elsewhere." Delete the memory reference. | fixed |
| TC-7 | form | medium | lines 63–70 | "Known non-findings" documents two cases the script never emits — defends against nothing. | Delete section; optionally one line under the table: "`_To be added_` markers and the template comment block are already filtered (`INTENTIONAL_COMMENT`) — they never appear in output." | fixed |
| TC-8 | form | low | lines 29–31 | Output contract shows no JSON instance; zero examples. | Append fenced block `[{"severity": "error", "id": "RQ_FN_07", "msg": "traces to UR_44 which does not exist in the workbook"}]` and note `id` semantics (item ID / bare prefix / literal `template`). | fixed |
| TC-9 | form | low | line 41 | `DC_*` row says "addressed by no expectation"; script flags a DC cited by no trace from *any* item. | Row label → `` `DC_*` gap that nothing traces to ``. | fixed |
| TC-10 | form | low | lines 29–30 | Exit code 2 (bad invocation) undocumented; could be read as "errors present". | Extend: "…`2` = bad invocation (no workbook path) — fix the command, do not route it as a finding." | fixed |

### install-toolkit

| ID | Category | Severity | Location | Finding | Proposal | Status |
|----|----------|----------|----------|---------|----------|--------|
| IT-1 | behavior | high | frontmatter | Description leads with a process summary; trigger list limited to three literal phrases; no failure symptoms (the realistic discovery path). | Replace with: `Use when the EE-Toolkit environment is not yet set up on this machine — the user asks to "install toolkit", "set up the toolkit", or "setup"; it is a first run after cloning/unzipping the repo; or a flow fails because uv, pandoc, or the Python environment is missing (uv: command not found, "pandoc is not recognized", ModuleNotFoundError: No module named 'docx', missing .venv, uv sync never run).` | approved |
| IT-2 | behavior | high | lines 13, 17–20 | PATH-refresh rule appears only at step 2 and does not cover step 1's detection commands — stale-PATH sessions misdiagnose working installs. | Move the refresh to a preamble covering *every* command: "**Windows first:** … prefix every command below with: `$env:Path = [Environment]::GetEnvironmentVariable('Path','Machine') + ';' + [Environment]::GetEnvironmentVariable('Path','User');`" Delete the now-redundant per-step "(with the prefix on Windows)". | approved |
| IT-3 | form | medium | lines 17–23 | Skip-prone PATH rule has no rationalization counter. | Add red-flag table: "`uv` is not recognized, so the install failed" → winget succeeded, session PATH is stale, re-run with prefix; "tell the user to restart the terminal" → never needed, use the prefix; "drop the prefix for this one command" → every uv/pandoc call needs it. | open |
| IT-4 | form | medium | line 23 | Step 5 is a vague prose reminder, not a completion contract. | Replace with: `5. **Report completion** with exactly these four lines: (a) uv <version> and pandoc <version> installed or already present; (b) environment created at .venv by uv sync; (c) verification: import docx, lxml → ok; (d) next step: no terminal restart needed — run "Execute flows/expectationeering-flow" now.` | open |
| IT-5 | behavior | medium | line 22 | Verification lacks expected output, failure branch, and the `.venv\Scripts\python.exe` artifact execute-flow depends on. | Extend step 4: verify (a) `uv run python -c "import docx, lxml; print('ok')"` prints ok; (b) `pandoc --version` prints a version line; (c) `.venv/Scripts/python.exe` (Windows) or `.venv/bin/python` exists — execute-flow calls it for trace-check. On failure: re-run with the PATH prefix before re-installing; still failing → report the failing command and output. | approved |
| IT-6 | form | low | line 11 | No overview line stating the core principle (install-once; Windows failure mode is stale PATH, not missing reboot). | Insert: "Two tools and one environment: uv (runs the scripts), pandoc (reads Word inputs), and .venv via uv sync. On Windows the only real failure mode is a stale PATH in the running session, never a needed reboot." | open |
| IT-7 | form | low | lines 13–16 | Three-OS installer mapping is a mixed-format bullet list; Linux pandoc instruction is prose. | Convert to table `\| OS \| uv \| pandoc \|`; Linux pandoc cell: `sudo apt install pandoc` (or `dnf install pandoc`). | open |
| IT-8 | form | low | line 19 vs execute-flow:29 | PATH-refresh snippet duplicated across both skills — intentional (execute-flow must run standalone). | Keep both; add pointer here: "(execute-flow uses this same prefix at run time.)" No execute-flow change. | open |
| IT-9 | form | low | line 13 | Detection commands left implicit. | "Check with `uv --version` and `pandoc --version` (with the Windows prefix); install only what is missing." | open |

## Deferred / dropped findings

(filled in Tasks 3–9: rejected by Eric, or baseline not reproduced)
